const config = require('../config.json');
const Discord = require('discord.js');
module.exports = {
	name: 'vote',
	description: 'Sends a link with instructions to bump the bot',
	emoji: '<:Up:859215855035744256>',
	execute(message, args) {
		
	},
};